# Model artifacts

`tfidf_features.joblib` is the fitted TF-IDF model used for application inference. It is generated reproducibly by `make features` and committed because it is small. Optional LoRA adapter files are generated by `scripts/finetune.py`; they must not include the base model and remain ignored.
